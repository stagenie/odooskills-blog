<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!--
        T18 — Héritage de vues
        Cas pratique : ajouter un onglet "Tickets" et un bouton statistique
        sur la fiche partenaire. On hérite de base.view_partner_form via
        inherit_id, et on combine plusieurs xpath avec position="inside",
        "attributes" et "after".
    -->
    <record id="view_partner_form_inherit_helpdesk" model="ir.ui.view">
        <field name="name">res.partner.form.inherit.odooskills.helpdesk</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">

            <!-- 1. Bouton statistique dans la boîte de boutons -->
            <xpath expr="//div[@name='button_box']" position="inside">
                <button name="%(action_helpdesk_ticket)d"
                        type="action"
                        class="oe_stat_button"
                        icon="fa-life-ring"
                        context="{'search_default_partner_id': id,
                                  'default_partner_id': id}">
                    <field name="ticket_count" widget="statinfo" string="Tickets"/>
                </button>
            </xpath>

            <!-- 2. Case "Client VIP" à côté du nom, via position="after" -->
            <xpath expr="//field[@name='category_id']" position="after">
                <field name="is_vip"/>
            </xpath>

            <!-- 3. Nouvel onglet "Tickets" dans le notebook (position="inside") -->
            <xpath expr="//notebook" position="inside">
                <page name="helpdesk_tickets" string="Tickets"
                      invisible="is_company == False and parent_id">
                    <field name="ticket_ids"
                           context="{'default_partner_id': id}">
                        <list decoration-danger="priority == '3'"
                              decoration-muted="state == 'done'">
                            <field name="reference"/>
                            <field name="name"/>
                            <field name="category_id" optional="show"/>
                            <field name="priority" widget="priority"/>
                            <field name="state"/>
                            <field name="create_date" optional="hide"/>
                        </list>
                    </field>
                </page>
            </xpath>

        </field>
    </record>

</odoo>
