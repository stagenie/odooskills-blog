<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- ============ Vue Liste ============ -->
    <record id="view_helpdesk_ticket_list" model="ir.ui.view">
        <field name="name">helpdesk.ticket.list</field>
        <field name="model">helpdesk.ticket</field>
        <field name="arch" type="xml">
            <list string="Tickets" decoration-danger="is_overdue"
                  decoration-success="state == 'done'"
                  decoration-muted="state == 'done'">
                <field name="reference"/>
                <field name="name"/>
                <field name="partner_id"/>
                <field name="category_id"/>
                <field name="user_id" widget="many2one_avatar_user"/>
                <field name="tag_ids" widget="many2many_tags" options="{'color_field': 'color'}"/>
                <field name="channel"/>
                <field name="deadline"/>
                <field name="is_overdue" invisible="1"/>
                <field name="state" widget="badge"
                       decoration-info="state == 'new'"
                       decoration-warning="state == 'in_progress'"
                       decoration-success="state == 'done'"/>
            </list>
        </field>
    </record>

    <!-- ============ Vue Formulaire ============ -->
    <record id="view_helpdesk_ticket_form" model="ir.ui.view">
        <field name="name">helpdesk.ticket.form</field>
        <field name="model">helpdesk.ticket</field>
        <field name="arch" type="xml">
            <form string="Ticket">
                <header>
                    <button name="action_start" type="object" string="Démarrer"
                            class="oe_highlight" invisible="state != 'new'"/>
                    <button name="action_resolve" type="object" string="Résoudre"
                            invisible="state != 'in_progress'"/>
                    <button name="action_open_close_wizard" type="object"
                            string="Clôturer (avec motif)" class="oe_highlight"
                            invisible="state == 'done'"/>
                    <button name="%(action_report_helpdesk_ticket)d" type="action"
                            string="Imprimer la fiche" icon="fa-print"/>
                    <field name="state" widget="statusbar" statusbar_visible="new,in_progress,done"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <label for="name" string="Sujet"/>
                        <h1><field name="name" placeholder="Ex : Imprimante bloquée en bourrage"/></h1>
                        <div>
                            <span class="text-muted">Référence : </span>
                            <field name="reference" readonly="1" class="o_inline_field"/>
                        </div>
                    </div>
                    <group>
                        <group>
                            <field name="partner_id"/>
                            <field name="category_id" options="{'no_create': True}"/>
                            <field name="user_id" widget="many2one_avatar_user"/>
                            <field name="channel"/>
                            <field name="is_urgent"/>
                        </group>
                        <group>
                            <field name="deadline"/>
                            <field name="resolved_at" readonly="1"
                                   invisible="state != 'done'"/>
                            <field name="is_overdue" readonly="1"
                                   invisible="not is_overdue"/>
                            <field name="sla_hours"/>
                            <field name="sla_status" widget="badge"
                                   decoration-success="sla_status == 'ok'"
                                   decoration-warning="sla_status == 'warning'"
                                   decoration-danger="sla_status == 'breach'"/>
                            <field name="currency_id" invisible="1"/>
                            <field name="estimated_cost"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Description" name="description">
                            <field name="description" placeholder="Décris le problème ici…"/>
                        </page>
                        <page string="Tags" name="tags">
                            <field name="tag_ids" widget="many2many_tags"
                                   options="{'color_field': 'color', 'no_create': False}"/>
                        </page>
                        <page string="Commentaires" name="comments">
                            <field name="comment_ids">
                                <list editable="bottom">
                                    <field name="author_id"/>
                                    <field name="body"/>
                                    <field name="create_date"/>
                                </list>
                            </field>
                        </page>
                        <page string="Résolution" name="resolution" invisible="state != 'done'">
                            <field name="resolution_note" placeholder="Note de clôture…"/>
                            <field name="hours_spent"/>
                        </page>
                    </notebook>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <!-- ============ Vue Kanban ============ -->
    <record id="view_helpdesk_ticket_kanban" model="ir.ui.view">
        <field name="name">helpdesk.ticket.kanban</field>
        <field name="model">helpdesk.ticket</field>
        <field name="arch" type="xml">
            <kanban default_group_by="state" group_edit="false">
                <field name="is_overdue"/>
                <field name="kanban_state"/>
                <progressbar field="kanban_state"
                             colors='{"done": "success", "normal": "muted", "blocked": "danger"}'/>
                <templates>
                    <t t-name="card" class="oe_kanban_card">
                        <div class="oe_kanban_details">
                            <div class="d-flex justify-content-between">
                                <strong class="o_kanban_record_title">
                                    <field name="reference"/>
                                </strong>
                                <field name="priority" widget="priority"/>
                            </div>
                            <div class="mt-1">
                                <field name="name"/>
                            </div>
                            <div class="text-muted small mt-1">
                                <field name="partner_id"/>
                                <span t-if="record.category_id.raw_value"> · </span>
                                <field name="category_id"/>
                            </div>
                            <div class="mt-2">
                                <field name="tag_ids" widget="many2many_tags"
                                       options="{'color_field': 'color'}"/>
                            </div>
                            <div class="d-flex justify-content-between align-items-center mt-2">
                                <div>
                                    <span t-if="record.deadline.raw_value"
                                          t-attf-class="badge #{record.is_overdue.raw_value ? 'text-bg-danger' : 'text-bg-light'}">
                                        <i class="fa fa-calendar" title="Échéance" role="img" aria-label="Échéance"/> <field name="deadline"/>
                                    </span>
                                </div>
                                <div>
                                    <field name="kanban_state" widget="state_selection"/>
                                    <field name="user_id" widget="many2one_avatar_user"/>
                                </div>
                            </div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <!-- ============ Vue Graph ============ -->
    <record id="view_helpdesk_ticket_graph" model="ir.ui.view">
        <field name="name">helpdesk.ticket.graph</field>
        <field name="model">helpdesk.ticket</field>
        <field name="arch" type="xml">
            <graph string="Analyse tickets" type="bar" stacked="1">
                <field name="category_id"/>
                <field name="state"/>
            </graph>
        </field>
    </record>

    <!-- ============ Vue Pivot ============ -->
    <record id="view_helpdesk_ticket_pivot" model="ir.ui.view">
        <field name="name">helpdesk.ticket.pivot</field>
        <field name="model">helpdesk.ticket</field>
        <field name="arch" type="xml">
            <pivot string="Pivot tickets">
                <field name="category_id" type="row"/>
                <field name="state" type="col"/>
                <field name="hours_spent" type="measure"/>
            </pivot>
        </field>
    </record>

    <!-- ============ Vue Recherche ============ -->
    <record id="view_helpdesk_ticket_search" model="ir.ui.view">
        <field name="name">helpdesk.ticket.search</field>
        <field name="model">helpdesk.ticket</field>
        <field name="arch" type="xml">
            <search string="Tickets">
                <field name="name" string="Sujet/Référence"
                       filter_domain="['|', ('name', 'ilike', self), ('reference', 'ilike', self)]"/>
                <field name="partner_id"/>
                <field name="category_id"/>
                <field name="user_id"/>
                <field name="tag_ids"/>

                <filter name="filter_mine" string="Mes tickets"
                        domain="[('user_id', '=', uid)]"/>
                <filter name="filter_unassigned" string="Non assignés"
                        domain="[('user_id', '=', False)]"/>
                <separator/>
                <filter name="filter_new" string="Nouveaux"
                        domain="[('state', '=', 'new')]"/>
                <filter name="filter_in_progress" string="En cours"
                        domain="[('state', '=', 'in_progress')]"/>
                <filter name="filter_done" string="Résolus"
                        domain="[('state', '=', 'done')]"/>
                <separator/>
                <filter name="filter_overdue" string="En retard"
                        domain="[('is_overdue', '=', True)]"/>
                <filter name="filter_urgent" string="Urgents"
                        domain="[('is_urgent', '=', True)]"/>

                <separator/>
                <filter name="group_state" string="Statut"
                        context="{'group_by': 'state'}"/>
                <filter name="group_category" string="Catégorie"
                        context="{'group_by': 'category_id'}"/>
                <filter name="group_user" string="Assigné à"
                        context="{'group_by': 'user_id'}"/>
                <filter name="group_channel" string="Canal"
                        context="{'group_by': 'channel'}"/>
            </search>
        </field>
    </record>

    <!-- ============ Action ============ -->
    <record id="action_helpdesk_ticket" model="ir.actions.act_window">
        <field name="name">Tickets</field>
        <field name="res_model">helpdesk.ticket</field>
        <field name="view_mode">kanban,list,form,graph,pivot</field>
        <field name="search_view_id" ref="view_helpdesk_ticket_search"/>
        <field name="context">{'search_default_filter_new': 1}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">Créer un premier ticket helpdesk</p>
            <p>Enregistre les demandes de support, assigne-les, suis les SLA et clôture-les depuis cet écran.</p>
        </field>
    </record>

</odoo>
