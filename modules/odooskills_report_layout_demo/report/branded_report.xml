<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- 1) Format papier A4 PAYSAGE, marges ajustées pour laisser respirer l'en-tête.
            header_spacing doit suivre la hauteur de l'en-tête, header_line trace un filet. -->
    <record id="paperformat_catalog_landscape" model="report.paperformat">
        <field name="name">Récapitulatif A4 paysage</field>
        <field name="format">A4</field>
        <field name="orientation">Landscape</field>
        <field name="margin_top">40</field>
        <field name="margin_bottom">20</field>
        <field name="margin_left">8</field>
        <field name="margin_right">8</field>
        <field name="header_spacing">35</field>
        <field name="header_line" eval="True"/>
        <field name="dpi">90</field>
    </record>

    <!-- 2) Action liée à ce format papier dédié -->
    <record id="action_report_branded_catalog" model="ir.actions.report">
        <field name="name">Récapitulatif (mise en page)</field>
        <field name="model">sale.order</field>
        <field name="report_type">qweb-pdf</field>
        <field name="report_name">odooskills_report_layout_demo.report_branded_catalog</field>
        <field name="report_file">odooskills_report_layout_demo.report_branded_catalog</field>
        <field name="paperformat_id" ref="paperformat_catalog_landscape"/>
        <field name="binding_model_id" ref="sale.model_sale_order"/>
        <field name="binding_type">report</field>
    </record>

    <!-- 3) Template : external_layout apporte logo, couleurs de marque et pied paginé.
            layout_document_title affiche un titre dans l'en-tête de la mise en page. -->
    <template id="report_branded_catalog">
        <t t-call="web.html_container">
            <t t-foreach="docs" t-as="doc">
                <t t-call="web.external_layout">
                    <t t-set="layout_document_title">Récapitulatif <span t-field="doc.name"/></t>
                    <div class="page">
                        <table class="table table-sm mt-3" style="border-top:2px solid #d6336c;">
                            <thead>
                                <tr>
                                    <th>Article</th>
                                    <th class="text-end">Qté</th>
                                    <th class="text-end">Prix unitaire</th>
                                    <th class="text-end">Total HT</th>
                                </tr>
                            </thead>
                            <tbody>
                                <tr t-foreach="doc.order_line" t-as="line">
                                    <td><span t-field="line.product_id"/></td>
                                    <td class="text-end"><span t-field="line.product_uom_qty"/></td>
                                    <td class="text-end"><span t-field="line.price_unit"/></td>
                                    <td class="text-end"><span t-field="line.price_subtotal"/></td>
                                </tr>
                            </tbody>
                        </table>
                        <div class="row justify-content-end mt-2">
                            <div class="col-3 text-end">
                                <strong>Total TTC : </strong>
                                <span t-field="doc.amount_total"/>
                            </div>
                        </div>
                    </div>
                </t>
            </t>
        </t>
    </template>

    <!-- 4) Personnalisation de la mise en page NATIVE : un filet d'accent sous l'en-tête.
            ⚠️ Héritage GLOBAL : touche tous les rapports en mise en page « Standard ». -->
    <template id="external_layout_standard_accent" inherit_id="web.external_layout_standard">
        <xpath expr="//h2[@t-out='layout_document_title']" position="before">
            <div style="height:3px;background:#d6336c;margin-bottom:8px;"/>
        </xpath>
    </template>

</odoo>
